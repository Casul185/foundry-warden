"""Foundry-Warden: a Windows game-mode daemon.

Detects Steam games, applies tiered throttling to background processes,
and optionally reports state to a telemetry endpoint (outbound only).
"""

__version__ = "0.1.0"
DEFAULT_NODE_NAME = "gaming-pc"
